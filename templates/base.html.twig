<!DOCTYPE html>
<html class="{{ current_theme() }}">
	<head>
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<link rel="icon" type="image/png" href="{{ asset(logo) }}">
		<script src="https://cdn.tailwindcss.com"></script>

        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
        {% endblock %}

		{% block javascripts %}
			{{ importmap('app') }}
		{% endblock %}
	</head>
	<body>

		<nav class="border-zinc-200">
			<div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
				<a class="flex items-center space-x-3">
					<img src="{{ asset(logo) }}" class="h-8" alt="Lumify Logo"/>
					<span class="self-center text-2xl font-bold text-white">Lumify</span>
				</a>
				<button data-collapse-toggle="navbar-default" type="button" class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-zinc-400 rounded-lg md:hidden hover:bg-zinc-700 focus:outline-none focus:ring-2 focus:ring-zinc-600">
					<span class="sr-only">Ouvrir le menu</span>
					<svg xmlns="http://www.w3.org/2000/svg" viewbox="0 0 448 512" fill="#FFF"><path d="M0 96C0 78.3 14.3 64 32 64l384 0c17.7 0 32 14.3 32 32s-14.3 32-32 32L32 128C14.3 128 0 113.7 0 96zM0 256c0-17.7 14.3-32 32-32l384 0c17.7 0 32 14.3 32 32s-14.3 32-32 32L32 288c-17.7 0-32-14.3-32-32zM448 416c0 17.7-14.3 32-32 32L32 448c-17.7 0-32-14.3-32-32s14.3-32 32-32l384 0c17.7 0 32 14.3 32 32z"/></svg>
				</button>
				<div class="hidden w-full md:block md:w-auto">
					<ul class="font-medium flex flex-col p-4 md:p-0 mt-4 md:flex-row md:space-x-8 md:mt-0">
						<li>
							<a href="/" class="block py-2 px-3 text-white text-rose-500 md:p-0 transition-all" aria-current="page">Accueil</a>
						</li>
						<li>
							<a href="/posts" class="block py-2 px-3 hover:text-rose-500 md:p-0 text-white transition-all">Publications</a>
						</li>
						<li>
							{% if app.user %}
								<a href="/users" class="block py-2 px-3 hover:text-rose-500 md:p-0 text-white transition-all">Profil</a>
							{% else %}
								<a href="{{ path('app_login') }}" class="block py-2 px-3 hover:text-rose-500 md:p-0 text-white transition-all">Profil</a>
							{% endif %}
						</li>
						{% if app.user is null %}
							<li>
								<a href="/register" class="block py-2 px-3 hover:text-rose-500 md:p-0 text-white transition-all">S'inscrire</a>
							</li>
							<li>
								<a href="/login" class="block py-2 px-3 hover:text-rose-500 md:p-0 text-white transition-all">Se connecter</a>
							</li>
						{% else %}
							<li>
								<a href="/logout" class="block py-2 px-3 hover:text-rose-500 md:p-0 text-white transition-all">Se déconnecter</a>
							</li>
						{% endif %}
					</ul>
				</div>
			</div>
			<hr class="border-zinc-700 mx-auto">
		</nav>
		{% block body %}{% endblock %}
	</body>
</html>
