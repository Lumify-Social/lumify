

<h1>Toutes les publications</h1>

{% for post in posts %}
    <div>
        <p><strong>{{ post.user.username }}</strong></p>
        <p>{{ post.content }}</p>
        <p><em>Publié le {{ post.createdAt|date('d/m/Y H:i') }}</em></p>
    </div>
{% else %}
    <p>Aucune publication à afficher.</p>
{% endfor %}